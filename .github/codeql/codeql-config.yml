name: "CodeQL config"

queries:
  - uses: security-and-quality

query-filters:
  - exclude:
      problem.severity:
        - warning
        - recommendation
  - exclude:
      id: go/log-injection

paths-ignore:
  - test
  - "**/*_test.go"
